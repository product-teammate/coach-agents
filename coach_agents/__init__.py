"""coach_agents package: operator-facing scripts for skills.

Skills invoke these modules via ``python -m coach_agents.scripts.<name>``.
The package itself is intentionally thin — shared logic lives in the
runtime and brain layers already.
"""
